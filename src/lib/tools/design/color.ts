export function hexRgb(hex:string){let h=hex.trim().replace('#','');if(h.length===3)h=[...h].map(x=>x+x).join('');if(!/^[\da-f]{6}$/i.test(h))throw new Error('Enter a 3 or 6 digit HEX color, such as #facc15.');return [0,2,4].map(i=>parseInt(h.slice(i,i+2),16)) as [number,number,number]}
export function hexToHsl(hex:string){const [r0,g0,b0]=hexRgb(hex),r=r0/255,g=g0/255,b=b0/255,max=Math.max(r,g,b),min=Math.min(r,g,b),d=max-min,l=(max+min)/2;let h=0,s=0;if(d){s=d/(1-Math.abs(2*l-1));h=max===r?60*(((g-b)/d)%6):max===g?60*((b-r)/d+2):60*((r-g)/d+4)}return `${Math.round((h+360)%360)}°, ${Math.round(s*100)}%, ${Math.round(l*100)}%`}
export function contrast(a:string,b:string){const lum=(h:string)=>{const c=hexRgb(h).map(v=>{v/=255;return v<=.04045?v/12.92:((v+.055)/1.055)**2.4});return .2126*c[0]+.7152*c[1]+.0722*c[2]};const [x,y]=[lum(a),lum(b)].sort((m,n)=>n-m);return (x+.05)/(y+.05)}
