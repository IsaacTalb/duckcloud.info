export function ipToInt(ip:string){const p=ip.trim().split('.').map(Number);if(p.length!==4||p.some(n=>!Number.isInteger(n)||n<0||n>255))throw new Error('Enter a valid IPv4 address with four numbers from 0 to 255.');return p.reduce((a,n)=>((a<<8)|n)>>>0,0)>>>0}
export const intToIp=(n:number)=>[24,16,8,0].map(s=>(n>>>s)&255).join('.');
export function calculateCidr(value:string){const [ip,prefixText]=value.trim().split('/');const prefix=Number(prefixText);if(!Number.isInteger(prefix)||prefix<0||prefix>32)throw new Error('CIDR prefix must be from 0 to 32, for example 192.168.1.0/24.');const n=ipToInt(ip),mask=prefix===0?0:(0xffffffff<<(32-prefix))>>>0,network=(n&mask)>>>0,broadcast=(network|(~mask>>>0))>>>0,total=2**(32-prefix),usable=prefix===32?1:prefix===31?2:Math.max(0,total-2);return {network:intToIp(network),broadcast:intToIp(broadcast),subnetMask:intToIp(mask),wildcardMask:intToIp(~mask>>>0),usableHosts:usable,firstUsable:intToIp(prefix>=31?network:network+1),lastUsable:intToIp(prefix>=31?broadcast:broadcast-1),binary:[24,16,8,0].map(s=>((n>>>s)&255).toString(2).padStart(8,'0')).join('.')};}
