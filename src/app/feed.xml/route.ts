import {getArticles} from '@/lib/cms';
const esc=(s:string='')=>s.replace(/[<>&'"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]!));
export async function GET(){const articles=await getArticles(1),items=articles.map(a=>`<item><title>${esc(a.title)}</title><link>https://duckcloud.info/blog/${esc(a.slug)}</link><guid>https://duckcloud.info/blog/${esc(a.slug)}</guid><description>${esc(a.excerpt||'')}</description>${a.published_at?`<pubDate>${new Date(a.published_at).toUTCString()}</pubDate>`:''}</item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Duck Cloud Blog</title><link>https://duckcloud.info/blog</link><description>Duck Cloud developer articles and guides.</description>${items}</channel></rss>`,{headers:{'content-type':'application/rss+xml; charset=utf-8','cache-control':'public, s-maxage=900'}})}
